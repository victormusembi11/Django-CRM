[tox]
envlist = 3.11
skipsdist = true
skip_missing_intepreters = true

[testenv]
deps = -r requirements.txt
passenv =
    SECRET_KEY
    DEBUG

commands =
    flake8
    coverage erase
    pytest --cov=./ ./
    coverage html
    coverage report --fail-under=100
